# History Unfolded v0.5

Adds evolving cultural axes, government revolutions, persistent battle records with distance-based army participation, and detailed artwork types and rarities.

## Build

```bash
npm install
npm run build
```

This version uses a new v5 browser save key because the simulation schema now includes battles and cultural traits.
